# 答辩手册 ——「行程站」火车订票系统

> 面向答辩人：clone 本仓库后，按第一部分从零跑起来，再按第三~五部分完成答辩汇报。
> 全程约 20 分钟准备 + 10 分钟答辩。

---

## 一、从零跑起来（拿到代码后照抄即可）

### 前置条件（只装两个软件）

1. **Python 3.10+**：官网 https://www.python.org/downloads/ 下载，安装时**勾选 "Add Python to PATH"**。
   验证：命令行输入 `python --version` 能显示版本号。
2. **MySQL 8.0+**：官网 https://dev.mysql.com/downloads/installer/ 下载社区版安装，记住你设置的 root 密码。
   验证：命令行输入 `mysql --version` 能显示版本号；若提示"不是内部或外部命令"，说明 mysql 没加入 PATH，后面用安装目录完整路径代替 `mysql`（常见位置如 `C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe`，以实际安装位置为准）。

### 第 1 步：克隆代码

```bash
git clone https://github.com/Derek-yyang/train-ticket-system.git
cd train-ticket-system
```

（仓库是私有的，clone 前需要仓库主人把你的 GitHub 账号加为协作者。）

### 第 2 步：创建虚拟环境并安装依赖

```bash
python -m venv .venv
.venv\Scripts\pip install -r requirements.txt
```

> - 第 1 条：`python -m venv .venv` 用 Python 自带的 venv 模块在当前目录创建一个叫 `.venv` 的隔离环境文件夹，之后装的包只进这个文件夹，不污染系统。
> - 第 2 条：明确使用 `.venv` 里的 pip，按 `requirements.txt` 清单批量安装依赖（Flask、PyMySQL、python-dotenv、cryptography）。
> - macOS/Linux 用户：把 `.venv\Scripts\` 换成 `.venv/bin/`。

### 第 3 步：配置数据库连接

在项目根目录复制模板并编辑：

```bash
copy .env.example .env
```

用记事本打开 `.env`，把 `MYSQL_PASSWORD=` 后面改成**你自己的 MySQL root 密码**：

```
MYSQL_USER=root
MYSQL_PASSWORD=你的密码
```

其余行（主机 127.0.0.1、端口 3306、库名 train_ticket_db）保持默认即可。

### 第 4 步：导入数据库（推荐：进一次 mysql，执行三个文件）

```bash
mysql -uroot -p
```

（mysql 不在 PATH 的话写完整路径；`-uroot` 是用户名连写；回车后输入密码，**屏幕上不显示是正常的**。）

进入 `mysql>` 提示符后，依次执行：

```sql
source database/schema.sql
source database/routines.sql
source database/seed.sql
exit
```

`source` 会执行对应文件里的全部 SQL。**顺序不能乱**：schema 建表 → routines 建存储过程/触发器 → seed 灌演示数据。

> - 三个文件都支持重复执行（内置防重复保护），答辩当天**重新导一遍**可以刷新演示日期——演示班次的日期是按"导入当天 +1/+2 天"动态生成的，重导后永远是未来两天，不会查不到车次。
> - 嫌来回敲麻烦也可以用三条独立命令（每条都要输一次密码）：
>   `mysql -uroot -p < database/schema.sql`，另外两个文件同理。`<` 表示把右边文件内容灌给 mysql 执行。

### 第 5 步：启动并验证

```bash
.venv\Scripts\python app.py
```

看到 `Running on http://127.0.0.1:5000` 即成功。浏览器打开 http://127.0.0.1:5000 ，首页能看到车站下拉框和近期班次。

**注意**：这个窗口要一直开着，它就是服务器；按 `Ctrl+C` 停止。

**演示账号**：

| 角色 | 用户名 | 密码 |
| --- | --- | --- |
| 管理员 | admin | Admin@123 |
| 乘客 | demo | Demo@123 |

### 常见故障排查

| 现象 | 原因与处理 |
| --- | --- |
| `Access denied for user 'root'` | 密码不对，检查 `.env` 和导入时输入的密码 |
| `Can't connect to MySQL server` | MySQL 服务没启动：Windows 服务里启动 MySQL80，或命令行 `net start MySQL80` |
| `mysql 不是内部或外部命令` | mysql 未加入 PATH，用安装目录完整路径，如 `"C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe" -uroot -p` |
| `ModuleNotFoundError: No module named 'cryptography'` | 依赖没装全，重跑 `pip install -r requirements.txt` |
| 启动报 `Address already in use` / 端口占用 | 5000 端口被占（可能已开了一个实例）：找到并关闭旧窗口再启动 |
| 页面能开但查询不到车次 | seed 没导入成功，重新执行 `source database/seed.sql` |
| 导入时中文报错或乱码 | 启动 mysql 客户端时加参数 `--default-character-set=utf8mb4` |

---

## 二、开场白（30 秒，照读）

> 各位老师好。我们小组的课题是火车订票系统"行程站"，技术栈是 Flask + MySQL 8。系统分乘客端和管理端：乘客可以查询任意两站之间的车次、选席别订票、管理订单；管理员维护车站、车次和班次，并查看销售统计。
> 我们这次设计的核心思路是：**把课设要求的数据库能力真正落在数据库层**——10 张表的完整约束体系、2 个视图、2 个存储过程、3 个触发器，并用事务加行级锁解决了并发超卖问题。下面我从数据库设计、关键实现和演示三部分来汇报。

---

## 三、数据库设计怎么讲（核心，3~4 分钟）

### 1. 表结构主线（先讲分组，再讲关系）

10 张表按业务分成四组，一条主线是"**静态资源 → 可售座位 → 订单**"：

- **用户域**：`app_users`（乘客/管理员，角色用 ENUM，密码存哈希）
- **静态资源域**：`stations` 车站 → `trains` 车次 → `train_stops` 途经站（带站序 stop_sequence）→ `carriages` 车厢（带席别）→ `train_seats` 物理座位
- **售票域**：`train_schedules` 某车次某天的班次；`schedule_seats` **班次×座位**实例，记录席别、票价、状态（AVAILABLE/SOLD/LOCKED）——这是整个系统的核心表，余票就是这张表里状态为 AVAILABLE 的行数
- **订单域**：`orders` 订单头 → `order_passengers` 订单明细（乘车人 + 具体哪个座位 + 票价）

讲设计亮点的一句话：
> "我们把'车'和'票'分开建模：`train_seats` 是物理座位，`schedule_seats` 是某天某班次上的座位实例，座位状态生命周期是 AVAILABLE → SOLD，取消后由触发器释放回 AVAILABLE。"

### 十张表逐表速查（建议背熟）

| # | 表 | 干什么 | 关键点 / 例子 |
| --- | --- | --- | --- |
| 1 | `app_users` 用户表 | 系统全部账号，乘客和管理员共用 | `role` 字段区分身份；密码只存哈希；用户名、身份证号唯一。例：admin / demo |
| 2 | `stations` 车站表 | 车站编码、名称、城市 | 同城站名不重复。例：广州南、北京西等 6 站 |
| 3 | `trains` 车次表 | 列车身份，与日期无关 | G1001 天天开，但只算一个车次。例：G1001、G6012、D2305 |
| 4 | `train_stops` 途经站表 | 某车次停靠哪些站、顺序、到发时刻 | 站序 `stop_sequence` 支撑任意区间查询。例：G1001 广州南→长沙南→武汉→北京西 |
| 5 | `carriages` 车厢表 | 某车次的车厢及席别 | 例：G1001 的 1 号商务座、2 号一等座、3 号二等座 |
| 6 | `train_seats` 物理座位表 | 每节车厢的座位号，铁路"固定资产" | 座位与日期无关。例：3 车 01A |
| 7 | `train_schedules` 班次表 | 某车次**某一天**的一次运行 | "车是模板，班次是实例"；控制开售/停售。例：8-31 的 G1001 |
| 8 | `schedule_seats` 班次座位实例表 | 可售票的雏形：这天这个座卖多少钱、什么状态 | **全系统核心表**；余票=AVAILABLE 行数；状态 AVAILABLE→SOLD（取消后触发器改回） |
| 9 | `orders` 订单表（头） | 一次购买：订单号、买家、班次、区间、金额、状态 | CHECK 保证出发站≠到达站；已取消不可恢复 |
| 10 | `order_passengers` 订单明细表 | 每张票：乘车人、证件、票价、具体座位 | 座位唯一键是防超卖最后防线；一个座位最多出现在一张票上 |

**连贯起来讲（一段话版本，背这段）：**

> "用户查票时，系统用 车站→车次→途经站 三张表算出线路，到 班次表 找到某天的班次，再数 班次座位表 里的 AVAILABLE 行数得出余票；用户下单时，存储过程在事务里把一个 AVAILABLE 座位改成 SOLD，往订单表写订单头、往明细表写乘车人票；用户退票时，订单状态改 CANCELED，触发器自动把座位改回 AVAILABLE。10 张表各司其职，靠外键串成一条完整的购票链路。"

### 2. 三大完整性（必问，对着 schema.sql 念实例）

| 课程要求 | 系统里的实例 |
| --- | --- |
| **实体完整性** | 所有表主键；`username`、`order_no`、`(schedule_id, seat_id)` 等唯一键防重复 |
| **参照完整性** | 订单同时外键关联用户、班次、出发站、到达站；明细外键关联订单和座位 |
| **用户定义完整性** | CHECK 约束：`出发站≠到达站`、`票价>0`、`车厢号 1~20`、`站序>0`；ENUM 限定状态取值 |

加分讲法：
> "用户定义完整性我们不止用了 CHECK。比如'一个座位只能出一张票'这条业务规则，是靠 `order_passengers.schedule_seat_id` 上的**唯一键**在数据库层兜底的——即使应用层有 bug 也不可能超卖。"

### 3. 视图（2 个）

- `v_train_schedule_search`：车次查询视图。把班次、车次、起止站、余票数、最低价聚合在一张视图里，**首页、查询页、详情页读的都是它**。设计亮点是 `train_stops` 自连接（`destination.stop_sequence > origin.stop_sequence`），支持像 12306 一样的**任意中途站区间查询**，不只是起点到终点。
- `v_daily_sales`：日销售统计视图（按日期+车次汇总已确认订单数、票数、销售额），管理端仪表盘直接读它。

### 4. 存储过程（重点讲 sp_create_order，最好配一页流程）

`sp_create_order` 的五步，建议背下来：

1. **校验区间**：查 `v_train_schedule_search`，班次不存在/停售/区间无效直接 SIGNAL 报错；
2. **锁定候选座位**：`SELECT ... LIMIT 1 FOR UPDATE`，对第一个 AVAILABLE 座位加行锁；
3. **条件更新**：`UPDATE ... SET seat_status='SOLD' WHERE schedule_seat_id=? AND seat_status='AVAILABLE'`，检查 `ROW_COUNT()=1`，不为 1 说明状态已变，抛错回滚；
4. **生成订单**：插入 orders（状态 CONFIRMED，金额=该座位票价）；
5. **生成车票**：插入 order_passengers，COMMIT。异常统一 `ROLLBACK + RESIGNAL`。

`sp_cancel_order`：先 `FOR UPDATE` 锁订单并校验"是本人的单"，再置 CANCELED；座位释放交给触发器。

### 5. 触发器（3 个）

- `trg_order_passengers_before_insert`：插入车票前校验座位必须是 SOLD 状态——**防止"没锁座就出票"**；
- `trg_orders_before_update`：已取消订单不允许恢复、不允许重复取消（状态机保护）；
- `trg_orders_after_update`：订单变为 CANCELED 后自动把关联座位释放回 AVAILABLE——**退票与余票联动**。

### 6. 并发控制（答辩最大亮点，1 分钟讲透）

> "如果只靠应用层先查余票再插订单，两个请求会同时读到'还有一张票'，造成超卖。我们把订票放进存储过程，开了三重防线：
> ① `FOR UPDATE` 行级锁，让并发请求在选定座位这一步串行化；
> ② UPDATE 带状态条件并校验影响行数，锁等待恢复后再确认座位仍是 AVAILABLE；
> ③ 订单明细表座位唯一键做数据库级兜底。
> 演示时我会用两个浏览器同时抢最后一张票，只有一个能成功。"
>
> 配合说明：InnoDB 默认 REPEATABLE READ 隔离级别；连接 autocommit=True 供普通查询使用，事务由存储过程内部显式 START TRANSACTION/COMMIT 管理。

### 7. 索引

- `idx_schedule_seats_search (schedule_id, seat_type, seat_status)`：查询页按班次+席别数余票走这个索引；
- `idx_orders_user_created`：我的订单按用户+时间倒序；
- `idx_orders_schedule_status`：统计某班次订单状态。

---

## 四、现场演示脚本（约 5 分钟）

> 边操作边说，每一步都有讲解词，不要沉默点鼠标。

**① 乘客订票（3 分钟）**
1. 打开首页："首页的数据来自车站表和余票视图，右侧展示近期可售班次。"
2. 用 demo 登录："登录用 Werkzeug 的 PBKDF2 哈希校验，数据库不存明文密码，会话用 Flask session。"
3. 查询 广州南 → 北京西："查询直接读视图 v_train_schedule_search，余票和最低价是视图聚合出来的。"
4. 进 G1001 详情："按席别分组统计余票，来自 schedule_seats 表。"
5. 选二等座，填乘车人（姓名 张三 / 身份证 18 位任意数字，如 440101200001010011），提交："这一步调用存储过程 sp_create_order，事务内锁座位、生成订单、生成车票。"
6. 到"我的订单"看订单："订单联查了 8 张表展示完整行程信息。"
7. **取消订单**："取消调用 sp_cancel_order，AFTER UPDATE 触发器自动把座位释放回 AVAILABLE。"
8. 回到查询页刷新："余票恢复 +1，证明退票和余票是数据库层联动的，不是应用层手改的。"

**② 并发抢票（1 分钟）**
9. 先查一遍余票，挑一个只剩 1 张的席别；开**两个浏览器窗口**（或普通+无痕窗口）都停在提交页；
10. 尽量同时提交："两个事务同时抢同一个座位，一个成功、一个收到数据库的报错提示——这就是 FOR UPDATE 行锁加条件更新的效果。"

**③ 管理端（1 分钟）**
11. 退出，用 admin 登录进后台："概览页的日销售统计直接读视图 v_daily_sales，最近订单联查订单链路。"
12. 车站管理：新增一个车站（如 编码 CQW / 重庆西 / 重庆）；再删一个已被引用的车站，演示报错："外键引用导致 DELETE 被数据库拒绝，所以系统提供'停用'代替物理删除。"
13. 班次管理：把某班次停售，再用乘客端查该班次——查不到："视图里过滤了 schedule_status='ON_SALE'，停售即时生效。"

---

## 五、可能被追问的问题（Q&A）

**Q1：为什么把订票逻辑放存储过程，而不是写在 Python 里？**
> 订票是竞争性操作，把"查-锁-改-插"放在数据库同一事务内，避免应用与数据库之间多次往返造成竞态窗口；也让事务边界清晰、所有客户端（未来的小程序、App）共享同一套规则。

**Q2：为什么订单状态直接是 CONFIRMED，还有 PENDING？**
> 表结构保留了 PENDING 待支付状态的设计扩展位；当前课程场景简化为确认即出票，所以存储过程直接置 CONFIRMED。

**Q3：你的隔离级别是什么？为什么不用 SERIALIZABLE？**
> InnoDB 默认 REPEATABLE READ。订票的关键路径靠 SELECT ... FOR UPDATE 当前读加行锁，配合状态条件更新，在默认隔离级别下已保证不超卖，不需要牺牲并发性换成表级更粗粒度的串行化。

**Q4：视图和直接写 JOIN 查询比有什么好处？**
> 三处页面复用同一段"可售班次"语义，避免口径不一致；对应用层屏蔽了 6 张表的连接细节；也便于统一加过滤条件（只看 ON_SALE）。

**Q5：如果两个用户给同一个乘车人订同一班次怎么办？**
> 当前模型允许（不同座位）；若业务上要求"同一证件同一班次只能一票"，可在 order_passengers 加 (passenger_id_number, schedule_id 相关) 的唯一约束扩展。

**Q6：删除车站为什么要报错？**
> 车站被 train_stops、orders 外键引用，InnoDB 默认 RESTRICT 阻止删除，保证参照完整性；运营上用停用（状态位）代替删除，历史订单仍可追溯。

**Q7：密码怎么存的？**
> Werkzeug generate_password_hash，默认 pbkdf2:sha256 加盐，校验用 check_password_hash，数据库只存哈希串。

**Q8：Locker 里的 LOCKED 状态什么时候用？**
> 预留给"选座锁定未支付"的扩展场景；当前流程在存储过程内一步到位 AVAILABLE→SOLD，LOCKED 未启用但保留了状态机完整性。

**Q9：测试怎么做的？**
> tests/ 下有无数据库依赖的静态校验（unittest），数据库约束本身由 MySQL 强制；演示中现场演示了外键拒绝删除、唯一键防重复、并发不超卖三个失败路径。

**Q10：项目不足与改进方向？**
> ① 订单号用时间戳+随机数，量大后应换序列或雪花算法；② 未做退票手续费/改签；③ 查询视图数据量大时可引入余票缓存表；④ 密码策略与登录限流未实现。这些是明确知道并愿意改进的点。

**Q11：你的设计满足第几范式？（任务书明确要求"分析规范程度"，必准备）**
> "各表均满足第三范式：非主属性完全函数依赖于候选键、不存在传递依赖。例如 `train_stops` 的到发时间只依赖于（车次，车站）本身；席别依赖车厢而非冗余在座位表；订单金额由明细票价汇总而来，不在订单外重复维护。
> 有两处**有意的冗余**：`schedule_seats.fare` 和 `order_passengers.ticket_price` 保存的是成交当时的票价快照——即使今后调价，历史订单金额也不会变。这是业务上正确的反范式设计，不是疏忽。"

**Q12：E-R 图只有 8 个实体，为什么有 10 张表？**
> "E-R 实体与表不是一一对应：`train_stops` 是'列车—车站'M:N 联系的属性化（站序、到发时间挂在联系上），落表时实现为独立表；`schedule_seats` 和 `order_passengers` 是联系实体/弱实体。所以图上 8 个实体框对应 10 张表，概念模型与逻辑模型是一致的。"
> （E-R 图见 `docs/assets/er-diagram.png`，报告中为"图 1"。）

---

## 六、答辩当天检查清单

- [ ] **确认用哪台电脑答辩**：优先自带笔记本（环境已就绪）；必须用教室电脑则提前一天装好 Python + MySQL 并完整试跑；保底准备一段演示录屏放在 U 盘
- [ ] 答辩前重新导入三个 SQL（刷新演示日期）
- [ ] `.env` 密码正确，`python app.py` 启动成功，首页可打开
- [ ] 两个浏览器窗口准备好（抢票演示）
- [ ] demo / Admin 两组账号密码记熟
- [ ] schema.sql / routines.sql 在编辑器里打开、定位到关键行（老师可能要看代码）
- [ ] E-R 图 `docs/assets/er-diagram.png` 提前打开（讲数据库设计时展示，配合 Q12 的说法）
- [ ] （加分项）现场跑一遍 `python -m unittest discover -s tests -v` 展示静态校验通过
- [ ] 关掉无关窗口和通知；浏览器收藏好 http://127.0.0.1:5000
- [ ] 万一现场连不上库：先看报错是否 Access denied（密码）或 Can't connect（服务没启动，`net start MySQL80`）
