# 答辩准备 —— 乘客端负责人（需求分析 / 乘客端界面 / 联调与测试）

> 配合《答辩讲解.md》使用：那份是全组通用手册；本文档只讲**你负责的部分**怎么讲、怎么答。
> 你在分工表中的职责：需求分析、乘客端查询与订票界面、登录注册与订单中心页面、前后端联调、乘客端测试。

---

## 一、你的分工对应到系统的哪些文件（答辩前必须能对号入座）

| 分工内容 | 对应文件 / 代码位置 |
| --- | --- |
| 需求分析 | 报告"一、需求说明"（角色表 + F01~F06 功能需求 + 非功能需求）；设计原则来自 PRODUCT.md |
| 首页 / 查询页 / 车次详情页 | `templates/home.html`、`search.html`、`trip_detail.html`；路由在 `app.py` 的 `/`、`/search`、`/trips/<id>` |
| 登录 / 注册 / 退出 | `templates/login.html`、`register.html`；路由 `/login`、`/register`、`/logout` |
| 我的订单 / 订票提交 / 取消 | `templates/orders.html`；路由 `POST /orders`（订票）、`GET /orders`（列表）、`POST /orders/<id>/cancel` |
| 界面样式与交互 | `static/css/app.css`、`static/js/app.js` |
| 联调测试 | 报告 T01~T05 测试用例；`tests/test_static_project.py` 静态校验 |

---

## 二、你的 2 分钟汇报脚本（全组汇报中的乘客端段落，背熟）

> "需求分析阶段我们确定了乘客和管理员两类角色。乘客的核心目标是**快速找到可售车次并完成可信的订票**，据此我们定了六项功能需求，其中我负责实现乘客端的 F01 身份认证、F02 车次查询、F03 在线订票和 F04 订单管理。
> 页面上，乘客从首页选择出发站、到达站和日期，查询结果直接来自数据库视图，展示余票和最低价；进入车次详情后按席别选择，填写乘车人信息提交，后端调用存储过程完成订票，全过程每一步都可以返回和验证。
> 我负责的前后端联调有三个要点：所有表单提交后用重定向刷新，防止重复提交；数据库返回的错误统一转换成用户能看懂的提示；未登录用户订票会被自动引导到登录页，登录后回到原页面。
> 测试方面，我按 T01 到 T05 执行了登录、查询、订票、取消和并发抢票用例，其中取消订单后余票自动恢复、两个窗口抢同一张票只有一人成功，这两个场景验证了数据库层的约束和锁真正生效。下面我现场演示乘客订票流程。"

（然后接《答辩讲解.md》第四章①的乘客订票演示——**那 3 分钟是你的主场**。）

---

## 三、必须吃透的 5 个技术点（老师最可能对着你的部分问）

### 1. 查询链路（app.py `/search`）

表单用 **GET** 提交三个参数（出发站、到达站、日期）→ 后端校验"出发站≠到达站" → 直接 `SELECT * FROM v_train_schedule_search WHERE ... ORDER BY departure_time, lowest_fare`。
要点：查询**不写任何业务 SQL 的 JOIN**，读的是视图——这是分工给我的接口，也是和数据库设计同学的协作点。

### 2. 注册校验的三个层次（亮点，主动讲）

- **前端**：HTML required 属性、密码框 type=password（第一道，但可绕过）；
- **后端**：用户名≥3 位、密码≥6 位、身份证 15 或 18 位、姓名非空（`app.py` register 路由）；
- **数据库**：username 唯一键、身份证唯一键，重复插入触发 MySQLError，被 except 捕获转成"用户名或身份证号已被使用"。

> 一句话："前端校验为了体验，后端校验为了安全，数据库约束做最终兜底——任何一层被绕过都不会产生脏数据。"

### 3. 登录与会话

`check_password_hash` 校验 PBKDF2 哈希 → 校验 `user_status='ACTIVE'` → `session["user_id"]` 存状态 → 每次请求 `before_request` 从 session 恢复 `g.current_user`。登出 `session.clear()`。

### 4. 订票链路与前后端交互模式

- 未登录点订票 → `@login_required` 装饰器拦截 → 重定向登录页并带 `?next=`，登录后跳回原页（讲得出 next 参数是加分项）；
- 提交 `POST /orders` → `cursor.callproc("sp_create_order", ...)` → 成功 flash 订单号，失败把 MySQL 报错转成中文提示；
- **PRG 模式**（POST → Redirect → GET）：订票成功后重定向到订单页，用户刷新不会重复下单；
- 模板数据来自 `context_processor`（current_user、席别/状态中文标签、today）。

### 5. 你负责的测试用例（T01~T05）

| 编号 | 场景 | 验证点 |
| --- | --- | --- |
| T01 | demo 登录 | 会话建立、导航变化 |
| T02 | 广州南→北京西查询 | 视图返回余票与最低价 |
| T03 | 订二等座 | 生成订单号、座位 AVAILABLE→SOLD |
| T04 | 取消订单 | 状态变 CANCELED、余票恢复（触发器联动） |
| T05 | 双窗口抢最后一张票 | 只有一个成功（FOR UPDATE 行锁） |

> 讲测试的句式："我不仅测了正常流程，还专门测了**失败路径**：重复注册、相同站查询、未登录订票、并发抢票——失败提示都来自数据库真实约束，不是我硬编码的。"

---

## 四、乘客端专属 Q&A（老师大概率问你的）

**Q1：查询为什么用 GET 而不是 POST？**
> 查询是幂等只读操作，GET 的结果页可刷新、可把链接发给朋友复现同样的搜索，也符合"读操作不改变状态"的语义。订票这种改变状态的操作才用 POST。

**Q2：用户没登录直接提交订票会怎样？**
> login_required 装饰器检测到 g.current_user 为空，flash 提示并重定向到登录页，同时把当前页面放进 next 参数，登录后自动跳回去，用户的操作不丢失。

**Q3：怎么防止用户刷新页面重复下单？**
> 订票接口用 POST-Redirect-GET：成功后立刻重定向到订单页，浏览器的地址栏变成 GET 请求，刷新只会重新读取订单列表，不会重复提交表单。

**Q4：搜索没有结果或出发到达站相同怎么办？**
> 相同站后端直接 flash 错误不查库；无结果时页面显示空状态，提示换个日期再试，不白屏也不报错——空状态和错误状态都有解释和下一步动作，这是我们的设计原则。

**Q5：联调中遇到过什么问题，怎么解决的？**
> 举两个真实例子：① 存储过程 SIGNAL 的错误最初直接把英文报错抛给用户，后来统一在 except 里取 error.args[1] 并映射成中文提示；② 日期参数最初允许过期日期，后端补了"默认今天"的处理。这类问题都是联调时前后端和数据库三方对齐解决的。

**Q6：界面设计上你们考虑了什么？**
> 三条原则：先呈现决策所需信息（站名、时间、余票、价格一眼可读）；流程每步可验证可返回；颜色不作为唯一状态表达（状态用文字加色点）。窄屏下查询和订票仍可完成，表格允许横向滚动但取消按钮永远可见。

**Q7：页面是前后端分离吗？**
> 不是，采用 Jinja 服务端渲染。理由：课设规模下服务端渲染链路短、无需再写一套接口层，且 flash 会话消息与模板天然配合；数据接口未来如果要接小程序，视图和存储过程可以直接复用。

**Q8：你们的界面是纯静态页吗，交互脚本做了什么？**
> static/js/app.js 负责轻量交互（如查询控件联动），核心数据都由服务端渲染，保证禁用 JavaScript 时流程仍可用。

---

## 五、"你具体做了什么"——分工答辩话术

老师常用分工表追问"这是不是你做的"。回答要**具体到文件和决策**，不要说"我做了前端"：

> "乘客端六个页面（首页、查询、详情、登录、注册、订单中心）的模板和样式是我写的，对应 app.py 里九条乘客端路由；需求分析时我起草了 F01~F06 功能表并和组员评审定稿；联调阶段我负责把数据库同学的视图和存储过程接到页面上，并设计了 flash 提示和 next 跳转的交互细节；测试用例 T01~T05 由我执行并整理进报告。数据库对象本身的定义是肖永荣负责，管理端是陈逸凡负责，我们通过代码走查保持接口一致。"

被追问细节时随手指得出代码：查询在 app.py 的 `/search` 路由，注册校验在 register 路由，测试用例在报告"（三）测试用例"表。

---

## 六、你的排练清单

- [ ] 完整走一遍乘客 demo（登录→查询→订票→取消→余票恢复），**这是你的演示段**，讲解词用《答辩讲解.md》第四章①
- [ ] 把 app.py 乘客端 9 条路由的代码读两遍，能说出每个路由的作用（行号定位）
- [ ] 注册三层校验、PRG、next 跳转、flash 机制——四个"自己的技术点"能脱稿讲
- [ ] T01~T05 亲手各做一遍，特别是双窗口抢票
- [ ] 准备好回答"没登录会怎样""刷新会不会重复下单""无结果怎么办"三个高频问题
- [ ] 答辩前重导 seed.sql 刷新演示日期（见通用手册第一章）
